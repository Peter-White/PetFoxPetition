<h1>Editing Comment</h1>

<%= form_for([@post, @comment]) do |f| %>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', comments_path %>
