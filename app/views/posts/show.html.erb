<p id="notice"><%= notice %></p>

<div class="showpost" id="showpost">
  <h3 id="showtitle">
    <strong>Title:</strong>
    <%= @post.title %>
  </h3>

  <p>
    <%= @post.content %>
  </p>
</div>

<div id="comments">
  <%= render @post.comments %>
</div>
<% if user_signed_in? %>
  <h3>Replay to thread</h3>
  <%= render "comments/form" %>
  <% if user_signed_in? %>
    <% if current_user.email === @post.user.email %>
      <%= link_to 'Edit', edit_post_path(@post) %> |
    <% end %>
    <% if current_user.email === 'pwhite007@icloud.com' %>
      <%= link_to 'Destroy', post_path(@post), method: :delete, data: { confirm: 'Are you sure?' } %> |
    <% end %>
  <% end %>
<% else %>
<h2>Please sign in to reply.</h2>
<% end %>
<%= link_to 'Back', posts_path %>
